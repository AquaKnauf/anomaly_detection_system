sqlalchemy
aiofiles
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# ML/AI libraries (install separately in Dockerfile to avoid conflicts)
# tensorflow==2.15.0  # Installed separately in Dockerfile
# torch>=2.0.0        # Installed separately in Dockerfile
# torchvision>=0.15.0 # Installed separately in Dockerfile

scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
prometheus-client>=0.19.0
redis>=5.0.0
kafka-python>=2.0.2
aiokafka>=0.8.11
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
joblib>=1.3.0